export interface TreeNode {
  val: number;
  left?: TreeNode;
  right?: TreeNode;
}

/**
 * @param {TreeNode} root
 * @param {number} sum
 * @return {boolean}
 */
export default function hasPathSum(
  root: TreeNode,
  sum: number
): boolean {

  return false;
}
